interface:
  display_name: "DCE Generation"
  short_description: "DCE Excel, quotation, PDF, import fallback, and release workflow guidance."
  default_prompt: "Use $dce-generation to modify DCE generated documents, sales quotations, Excel/PDF exports, import fallback logic, UI panels, or release packages safely."
