# AGENTS.md

本文件是 AI Agent 的项目入职手册。任何 AI 工具在参与项目开发前，必须先阅读并遵守这些规则。

## 项目说明

请在接入具体项目后补充：

- 项目名称：
- 项目用途：
- 目标用户：
- 核心功能：
- 当前阶段：

## 技术栈

请在接入具体项目后补充：

- 语言：
- 框架：
- 数据库：
- 包管理器：
- 构建工具：
- 测试工具：

## 常用命令

请在接入具体项目后补充：

```text
安装依赖：
启动开发：
运行测试：
构建：
格式化：
发布：
```

## 工作方式

- 修改前先阅读相关代码和文档。
- 不要凭空假设项目结构、接口或业务规则。
- 不要修改无关文件。
- 优先小步修改，避免一次性大范围重构。
- 遇到不确定问题，先列出判断点，不要擅自决定。
- 完成后说明修改文件、核心逻辑、测试结果和剩余风险。

## 系统演进规则

- 通用模板只加入跨项目、跨 AI 工具都适用的规则。
- 单个项目的特殊规则留在该项目自己的 `AGENTS.md`，不要回灌到通用模板。
- 一次性问题只记录到 `docs/ai-work-log.md`。
- 反复出现 2 次以上的问题，才考虑沉淀到 `docs/project-memory.md`。
- 反复出现 3 次以上且跨项目适用的问题，才考虑进入通用模板。
- 新增规则必须能回答：它防止什么错误、何时触发、放在哪个文件最合适。
- 如果规则超过 5 行，优先考虑拆成 checklist 或 prompt，不要塞进主规则。
- 家里、公司、多电脑同步时，先更新模板仓库，再同步到目标项目。
- 同步到公司项目时，不要覆盖公司项目已有业务规则，除非用户明确要求。

## Git 规则

- 修改前先查看当前分支和工作区状态。
- 不允许擅自执行 `git reset --hard`、`git checkout -- <file>` 等会丢失改动的命令。
- 不允许覆盖用户已有改动。
- 不允许修改 Git 历史，除非用户明确要求。
- 提交信息应说明变更目的和影响范围。

## 代码规则

- 遵守项目现有风格。
- 优先使用项目已有工具、函数、组件和目录结构。
- 不新增不必要的抽象。
- 不引入无关依赖。
- 不隐藏错误，不吞掉异常。
- 涉及用户数据、权限、安全、支付、发布的改动必须先请求审批。

## 测试规则

- 能写测试时优先写测试。
- 修 bug 时优先补复现测试。
- 修改后运行相关测试。
- 如果无法运行测试，必须说明原因和替代验证方式。
- 不允许为了让测试通过而改变业务协议或验收标准。

## 安全与红线

AI 不得擅自执行以下动作：

- 删除数据或清空目录。
- 修改生产配置。
- 提交密码、Token、API Key、`.env`。
- 修改支付、权限、合规、审计逻辑。
- 发布生产版本。
- 发送外部消息、邮件、客户通知。
- 大范围重构或迁移。

## 输出格式

完成任务后必须输出：

```text
改动摘要：
修改文件：
测试结果：
剩余风险：
需要人工确认：
```
